if has('gui_running')
  set background=light
else
  set background=light
endif
let g:solarized_termcolors=256
colorscheme solarized

" Integrate vim-fugitive with Bluebox Github Enterprise install
let g:fugitive_github_domains = ['https://github.blueboxgrid.com']

